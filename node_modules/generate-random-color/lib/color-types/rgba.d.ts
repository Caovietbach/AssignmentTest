declare type RGBA = {
  r?: {
    min: number;
    max: number;
  };
  g?: {
    min: number;
    max: number;
  };
  b?: {
    min: number;
    max: number;
  };
  a?: {
    min: number;
    max: number;
  };
};
export default function (input?: RGBA): string;
export {};
