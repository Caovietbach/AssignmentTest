'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
var generateNum_1 = require('../helpers/generateNum');
function generateHex() {
  var generate = function () {
    var value = generateNum_1.default(16, { min: 0, max: 15 });
    return convertNumberToHex(value);
  };
  return '' + generate() + generate();
}
function convertNumberToHex(input) {
  switch (input) {
    case 10:
      return 'A';
    case 11:
      return 'B';
    case 12:
      return 'C';
    case 13:
      return 'D';
    case 14:
      return 'E';
    case 15:
      return 'F';
    default:
      return input.toString();
  }
}
function default_1(input) {
  var r = (input === null || input === void 0 ? void 0 : input.r) ? input.r : generateHex();
  var g = (input === null || input === void 0 ? void 0 : input.g) ? input.g : generateHex();
  var b = (input === null || input === void 0 ? void 0 : input.b) ? input.b : generateHex();
  return '#' + r.toUpperCase() + g.toUpperCase() + b.toUpperCase();
}
exports.default = default_1;
