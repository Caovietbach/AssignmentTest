'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
function default_1(trueMax, numRange) {
  var max =
    (numRange === null || numRange === void 0 ? void 0 : numRange.max) != undefined && numRange.max <= trueMax
      ? numRange.max
      : trueMax;
  var min =
    (numRange === null || numRange === void 0 ? void 0 : numRange.min) != undefined && numRange.min >= 0
      ? numRange.min
      : 0;
  return parseFloat((Math.random() * (max - min) + min).toFixed(2));
}
exports.default = default_1;
